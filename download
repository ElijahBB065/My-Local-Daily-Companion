# Local account database -- created automatically by accounts.py on first
# run. Never commit this: it holds real (hashed) pilot-tester credentials.
accounts.db

# Streamlit secrets (OpenAQ API key, etc.)
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
venv/
.venv/
